[tools]
bun = "1.3.10"
node = "24"

[tasks.pre-commit]
description = "Lint staged files"
run = """
STAGED=$(git diff --cached --name-only --diff-filter=ACMR -- '*.ts' '*.tsx' '*.js' '*.jsx' '*.json')
if [ -n "$STAGED" ]; then
  echo "$STAGED" | xargs bunx eslint --cache
fi
"""

[tasks.commit-msg]
description = "Validate commit message"
run = "bunx commitlint --edit"

[tasks.setup]
description = "Install git hooks"
run = [
  "mise generate git-pre-commit --write",
  "mise generate git-pre-commit --write --hook=commit-msg --task=commit-msg",
]
